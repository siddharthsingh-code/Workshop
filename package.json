{
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "tailwindcss": "^4.3.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "scripts": {
    "frontend": "cd workshop && npm run dev",
    "backend": "cd backend && nodemon server.js",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  }
}
